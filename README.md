Assignment 2 — ArrayList & LinkedList Data Structures

Interface — MyList<T>
MethodDescriptionadd(T item)Append to endadd(int index, T item)Insert at positionset(int index, T item)Replace elementget(int index)Retrieve elementindexOf(Object item)First occurrence indexlastIndexOf(Object item)Last occurrence indexexists(Object item)Membership testremove(int index)Remove by positionremove(Object item)Remove by valueclear()Remove all elementssize()Element counttoArray()Snapshot as Object[]sort()Natural-order sortiterator()Forward iterator
